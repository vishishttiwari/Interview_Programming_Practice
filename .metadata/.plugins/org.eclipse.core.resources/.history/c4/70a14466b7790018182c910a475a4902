package util;

public class Graph {
	public int vertices;
	public Node[] adjacencyList;
	
	public Graph(int[] vertices) {
		this.vertices = vertices.length;
		adjacencyList = new Node[vertices.length];
		for (int i = 0; i < vertices.length; i++)
			adjacencyList[i] = new Node(vertices[i]);
	}
}
