package iterators;

import java.util.Iterator;

public class Stack<T> {
	public void iterate(Stack<Integer> stack) {
		Iterator itr = Iter 
		while(itr.hasNext()) {  
			System.out.println(itr.next());  
		}
	}
}
