package search;

import util.Graph;
import util.Node;

public class Node_Search {
	public static Node search(Graph graph, int value) {
		for (int i = 0; i < graph.adjacencyList.length; i++)
			if (graph.adjacencyList[i].value == value) return graph.adjacencyList[i];
		return null;
	}
}
